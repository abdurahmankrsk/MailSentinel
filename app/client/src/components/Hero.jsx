export default function Hero() {
  return (
    <section className="hero">
      <h2 className="hero-title">Every score, fully explained.</h2>
      <p className="hero-sub">
        Paste a raw email or a link below. MailSentinel runs it through DNS, domain, and link
        forensics, and shows you exactly which checks passed, which failed, and why. It's free,
        and nothing you paste is ever stored.
      </p>
    </section>
  )
}
